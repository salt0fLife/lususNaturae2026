[gd_scene load_steps=10 format=3 uid="uid://btqhlme3tqi1f"]

[ext_resource type="Script" path="res://campaign/levels/dynamic_environment.gd" id="1_bt8x0"]
[ext_resource type="Texture2D" uid="uid://b5gnyaki517i8" path="res://assets/textures/sky/stars/shooting_star_1.png" id="2_wplnj"]
[ext_resource type="PackedScene" uid="uid://byjn5yc1m1okl" path="res://addons/SunshineVolumetricClouds/CloudsPrefab.tscn" id="3_uwqub"]
[ext_resource type="Script" path="res://scripts/spinning_sun.gd" id="4_nlc57"]
[ext_resource type="PackedScene" uid="uid://rggfr8yhq4vg" path="res://assets/effects/fancy_lens_flare.tscn" id="5_ayvvx"]

[sub_resource type="Shader" id="Shader_hcxqm"]
code = "//
//// NOTE: Shader automatically converted from Godot Engine 4.2.1.stable's PhysicalSkyMaterial.
//
//shader_type sky;
//render_mode use_debanding;
//
shader_type sky;
render_mode use_half_res_pass;

group_uniforms physical_sky;
uniform float physical_effect : hint_range(0.0, 1.0, 0.01) = 0.0;

uniform float rayleigh : hint_range(0, 64) = 2.0;
uniform vec4 rayleigh_color : source_color = vec4(0.3, 0.405, 0.6, 1.0);
uniform float mie : hint_range(0, 1) = 0.005;
uniform float mie_eccentricity : hint_range(-1, 1) = 0.8;
uniform vec4 mie_color : source_color = vec4(0.69, 0.729, 0.812, 1.0);

uniform float turbidity : hint_range(0, 1000) = 10.0;
uniform float sun_disk_scale : hint_range(0, 360) = 1.0;
uniform vec4 ground_color : source_color = vec4(0.1, 0.07, 0.034, 1.0);
uniform float exposure : hint_range(0, 128) = 1.0;

const vec3 UP = vec3( 0.0, 1.0, 0.0 );

// Optical length at zenith for molecules.
const float rayleigh_zenith_size = 8.4e3;
const float mie_zenith_size = 1.25e3;

float henyey_greenstein(float cos_theta, float g) {
	const float k = 0.0795774715459;
	return k * (1.0 - g * g) / (pow(1.0 + g * g - 2.0 * g * cos_theta, 1.5));
}

group_uniforms sky_style;

uniform vec3 top_color : source_color = vec3(1.0);
uniform vec3 bottom_color : source_color = vec3(1.0);
uniform vec3 sun_scatter : source_color = vec3(1.0);

group_uniforms astro;

uniform vec3 astro_tint : source_color;
uniform sampler2D astro_sampler : repeat_disable, filter_linear_mipmap;
uniform float astro_scale : hint_range(0.1, 10.0, 0.1) = 1.0;
uniform float astro_intensity : hint_range(1.0, 3.0, 0.1) = 1.0;

group_uniforms stars;

uniform float stars_intensity : hint_range(0.0, 5.0, 0.1) = 0.0;

group_uniforms shooting_stars;

uniform float shooting_stars_intensity : hint_range(0.0, 10.0, 0.1) = 0.0;
uniform sampler2D shooting_star_sampler : filter_linear, repeat_disable;
uniform vec3 shooting_star_tint : source_color;
uniform int shooting_star_count = 4;
uniform float shooting_star_cycle_length = 1.0;
uniform float shooting_star_cycle_roof : hint_range(-1.0, 1.0, 0.01) = 0.9;
uniform float shooting_star_duration = 1.0;
uniform float shooting_star_size = 1.0;

float rand(float n){return fract(sin(n) * 43758.5453123);}

// Voronoi method credit:
// The MIT License
// Copyright © 2013 Inigo Quilez
// https://www.shadertoy.com/view/ldl3Dl

vec3 hash( vec3 x ){
	x = vec3( dot(x,vec3(127.1,311.7, 74.7)),
			  dot(x,vec3(269.5,183.3,246.1)),
			  dot(x,vec3(113.5,271.9,124.6)));
	return fract(sin(x)*43758.5453123);
}

vec3 voronoi( in vec3 x ){
	vec3 p = floor( x );
	vec3 f = fract( x );
	
	float id = 0.0;
	vec2 res = vec2( 100.0 );
	for( int k=-1; k<=1; k++ )
	for( int j=-1; j<=1; j++ )
	for( int i=-1; i<=1; i++ ) {
		vec3 b = vec3( float(i), float(j), float(k) );
		vec3 r = vec3( b ) - f + hash( p + b );
		float d = dot( r, r );
		if( d < res.x ) {
			id = dot( p+b, vec3(1.0,57.0,113.0 ) );
			res = vec2( d, res.x );
		} else if( d < res.y ) {
			res.y = d;
		}
    }
    return vec3( sqrt( res ), abs(id) );
}

// https://stackoverflow.com/questions/18558910/direction-vector-to-rotation-matrix

mat3 direction_to_matrix(vec3 direction) {
	vec3 x_axis = normalize(cross(vec3(0.0, 1.0, 0.0), direction));
	vec3 y_axis = normalize(cross(direction, x_axis));
	return mat3(vec3(x_axis.x, y_axis.x, direction.x),
				vec3(x_axis.y, y_axis.y, direction.y),
				vec3(x_axis.z, y_axis.z, direction.z));
}

vec3 random_direction(float seed){
	float phi = rand(seed) * PI;
	float costheta = rand(seed + 100.0) * 2.0 - 1.0;
	float theta = acos(costheta);
	return vec3( sin(theta) * cos(phi), (theta) * sin(phi), cos(theta) );
}

float get_shooting_star(vec3 eyedir){
	float shooting_star = 0.0;
	if (sin(TIME*shooting_star_cycle_length) < shooting_star_cycle_roof){
		return 0.0;
	}
	for(int i = 0; i < shooting_star_count; i++){
		float base_rand = rand(float(i));
		float time = TIME + base_rand * 2.0;
		float duration = (0.5 + base_rand)*shooting_star_duration;
		float seed = floor(time / duration) * duration + base_rand;
		float progress = mod(time, duration) / duration;
		float rand_value = rand(seed + 100.0);
		float rand_scale = base_rand * 10.0;
		float a = rand_value * 0.5;
		mat3 angle = mat3(vec3(cos(a), -sin(a), 0.0), vec3(sin(a), cos(a), 0.0), vec3(0.0, 0.0, 1.0));
		vec3 shooting_dir = direction_to_matrix(random_direction(seed)) * angle * eyedir;
		vec2 shooting_uv = ((shooting_dir.xy + vec2(0.0, progress * 0.4*shooting_star_duration)) * (8.0 + rand_scale)*shooting_star_size) + vec2(0.5);

		float shooting_mask = ceil(
			clamp(shooting_uv.x * (1.0 - shooting_uv.x), 0.0, 1.0) *
			clamp(shooting_uv.y * (1.0 - shooting_uv.y), 0.0, 1.0)
			) * ceil(shooting_dir.z);
			
		shooting_star = clamp(
			shooting_star + texture(shooting_star_sampler, shooting_uv).a
			* sin(progress * PI)
			* shooting_mask * rand_value,
		0.0, 1.0);
	}
	return clamp(shooting_star, 0.0, 1.0);
}

void sky() {
	
	float horizon_mask = abs(EYEDIR.y);
	float bottom_mask = smoothstep(0.5, 0.45, SKY_COORDS.y);
	
	vec3 dir = direction_to_matrix(LIGHT0_DIRECTION) * EYEDIR;
	vec2 astro_uv = (-(dir.xy / dir.z) * astro_scale) + vec2(0.5);
	float astro_mask = ceil(
		clamp(astro_uv.x * (1.0 - astro_uv.x), 0.0, 1.0) *
		clamp(astro_uv.y * (1.0 - astro_uv.y), 0.0, 1.0)
		) * ceil(dir.z);
	vec4 astro_color = texture(astro_sampler, astro_uv);
	
	// Sky color
	
	vec3 sky_gradient = mix(bottom_color.rgb, top_color.rgb, clamp(EYEDIR.y, 0.0, 1.0));
	vec3 sunset_color = sun_scatter * (1.0 - horizon_mask);
	vec3 sky_color = clamp(sky_gradient + sunset_color, 0.0, 1.0);
	
	// Stars
	
	if(stars_intensity > 0.0){
		vec2 stars = voronoi(EYEDIR * 25.0).xz;
		sky_color += smoothstep(0.025 + ((1.0 + sin(TIME + stars.y)) / 2.0) * 0.05, 0.0, stars.x) * stars_intensity;
	}
	
	// Add shooting stars
	
	sky_color += get_shooting_star(EYEDIR) * shooting_stars_intensity * shooting_star_tint;
	
	// Add astro
	
	sky_color = mix(sky_color, astro_color.rgb * astro_intensity * astro_tint, astro_color.a * astro_mask * bottom_mask);
	
	
	COLOR = sky_color;
	
	if (LIGHT0_ENABLED) {
		float zenith_angle = clamp( dot(UP, normalize(LIGHT0_DIRECTION)), -1.0, 1.0 );
		float sun_energy = max(0.0, 1.0 - exp(-((PI * 0.5) - acos(zenith_angle)))) * LIGHT0_ENERGY;
		float sun_fade = 1.0 - clamp(1.0 - exp(LIGHT0_DIRECTION.y), 0.0, 1.0);

		// Rayleigh coefficients.
		float rayleigh_coefficient = rayleigh - ( 1.0 * ( 1.0 - sun_fade ) );
		vec3 rayleigh_beta = rayleigh_coefficient * rayleigh_color.rgb * 0.0001;
		// mie coefficients from Preetham
		vec3 mie_beta = turbidity * mie * mie_color.rgb * 0.000434;

		// Optical length.
		float zenith = acos(max(0.0, dot(UP, EYEDIR)));
		float optical_mass = 1.0 / (cos(zenith) + 0.15 * pow(93.885 - degrees(zenith), -1.253));
		float rayleigh_scatter = rayleigh_zenith_size * optical_mass;
		float mie_scatter = mie_zenith_size * optical_mass;

		// Light extinction based on thickness of atmosphere.
		vec3 extinction = exp(-(rayleigh_beta * rayleigh_scatter + mie_beta * mie_scatter));

		// In scattering.
		float cos_theta = dot(EYEDIR, normalize(LIGHT0_DIRECTION));

		float rayleigh_phase = (3.0 / (16.0 * PI)) * (1.0 + pow(cos_theta * 0.5 + 0.5, 2.0));
		vec3 betaRTheta = rayleigh_beta * rayleigh_phase;

		float mie_phase = henyey_greenstein(cos_theta, mie_eccentricity);
		vec3 betaMTheta = mie_beta * mie_phase;

		vec3 Lin = pow(sun_energy * ((betaRTheta + betaMTheta) / (rayleigh_beta + mie_beta)) * (1.0 - extinction), vec3(1.5));
		// Hack from https://github.com/mrdoob/three.js/blob/master/examples/jsm/objects/Sky.js
		Lin *= mix(vec3(1.0), pow(sun_energy * ((betaRTheta + betaMTheta) / (rayleigh_beta + mie_beta)) * extinction, vec3(0.5)), clamp(pow(1.0 - zenith_angle, 5.0), 0.0, 1.0));

		// Hack in the ground color.
		Lin  *= mix(ground_color.rgb, vec3(1.0), smoothstep(-0.1, 0.1, dot(UP, EYEDIR)));

		// Solar disk and out-scattering.
		float sunAngularDiameterCos = cos(LIGHT0_SIZE * sun_disk_scale);
		float sunAngularDiameterCos2 = cos(LIGHT0_SIZE * sun_disk_scale*0.5);
		float sundisk = smoothstep(sunAngularDiameterCos, sunAngularDiameterCos2, cos_theta);
		
		vec3 L0 = (sun_energy * extinction) * sundisk * LIGHT0_COLOR;
		//L0 += texture(night_sky, SKY_COORDS).xyz * extinction;
		
		vec3 color = Lin + L0;
		COLOR = mix(exposure * COLOR * pow(color, vec3(1.0 / (1.2 + (1.2 * sun_fade)))), COLOR, 1.0-physical_effect);
		
		
		//COLOR *= exposure;
	} else {
		// There is no sun, so display night_sky and nothing else.
		//COLOR = texture(night_sky, SKY_COORDS).xyz;
		COLOR *= exposure;
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_b4e47"]
shader = SubResource("Shader_hcxqm")
shader_parameter/physical_effect = 1.0
shader_parameter/rayleigh = 2.0
shader_parameter/rayleigh_color = Color(0.454902, 0.564706, 0.768627, 1)
shader_parameter/mie = 0.1444
shader_parameter/mie_eccentricity = 0.532
shader_parameter/mie_color = Color(0.690196, 0.729412, 0.811765, 1)
shader_parameter/turbidity = 10.0
shader_parameter/sun_disk_scale = 1.0
shader_parameter/ground_color = Color(0.396078, 0.45098, 0.835294, 1)
shader_parameter/exposure = 2.273
shader_parameter/top_color = Color(0.235294, 0.27451, 0.286275, 1)
shader_parameter/bottom_color = Color(0.184314, 0.219608, 0.168627, 1)
shader_parameter/sun_scatter = Color(0.0941176, 0.0666667, 0.0745098, 1)
shader_parameter/astro_tint = Color(1, 1, 1, 1)
shader_parameter/astro_scale = 5.2
shader_parameter/astro_intensity = 1.0
shader_parameter/stars_intensity = 1.8
shader_parameter/shooting_stars_intensity = 10.0
shader_parameter/shooting_star_tint = Color(1, 1, 1, 1)
shader_parameter/shooting_star_count = 3
shader_parameter/shooting_star_cycle_length = 0.1
shader_parameter/shooting_star_cycle_roof = 0.99
shader_parameter/shooting_star_duration = 2.0
shader_parameter/shooting_star_size = 2.885
shader_parameter/shooting_star_sampler = ExtResource("2_wplnj")

[sub_resource type="Sky" id="Sky_5262a"]
sky_material = SubResource("ShaderMaterial_b4e47")

[sub_resource type="Environment" id="Environment_fknbc"]
background_mode = 2
sky = SubResource("Sky_5262a")
ambient_light_source = 2
ambient_light_color = Color(0.0941176, 0.113725, 0.0823529, 1)
ambient_light_energy = 2.0
ssao_enabled = true
ssao_intensity = 1.0
ssao_light_affect = 0.5
ssao_ao_channel_affect = 1.0
glow_enabled = true
glow_blend_mode = 0

[node name="dynamic_environment" type="Node3D"]
script = ExtResource("1_bt8x0")
set_preset = "night"

[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
environment = SubResource("Environment_fknbc")

[node name="CloudsSystem" parent="." node_paths=PackedStringArray("sunLight", "worldEnvironment") instance=ExtResource("3_uwqub")]
updateConstantly = true
sunLight = NodePath("")
worldEnvironment = NodePath("")
windSpeed = 0.005
cloudsCutoff = -0.045
cloudsFloor = -1000.0
cloudsCeiling = 1000.0
detailNoiseScale = 4.476
detailNoisePower = 0.753
largeScaleNoiseScale = 0.416
sunColorDefault = Color(0.0655133, 0.0695118, 0.0784929, 1)
overrideAmbientLight = true
ambientColorDefault = Color(0, 0.00392157, 0.00392157, 1)
useFogDefault = false
fogColorDefault = Color(0.518, 0.553, 0.608, 1)
sunColorMult = Color(0.0784314, 0.0784314, 0.0862745, 1)
cloudShadowStrength = 0.0
shape_speed_multiplier = 0.0
shape_starting_offset = Vector3(0.105, 0, 0)

[node name="DirectionalLight3D2" type="DirectionalLight3D" parent="." groups=["light"]]
transform = Transform3D(-0.368951, -0.601057, -0.708946, 0.311367, -0.798615, 0.515038, -0.875743, -0.0307186, 0.481799, 0, 0, 0)
light_color = Color(0.835294, 0.886275, 0.909804, 1)
shadow_enabled = true
script = ExtResource("4_nlc57")

[node name="fancy_lens_flare" parent="DirectionalLight3D2" instance=ExtResource("5_ayvvx")]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0, 400)
attunation_mult = 0.05
use_position_as_offset = true
